# rspamd Controller — Passwort für das Web-UI und API
# Generiere mit: rspamadm pw --encrypt
# Standardmäßig deaktiviert (nur internes Docker-Netzwerk erreichbar)
password = "";
enable_password = "";
bind_socket = "0.0.0.0:11334";
